export class Category {
  id: string;
  name: string;
  backgroundColor: string;

  constructor(
    id: string,
    name: string,
    backgroundColor: string
  ) {
    this.id = id;
    this.name = name;
    this.backgroundColor = backgroundColor;
  }
}
